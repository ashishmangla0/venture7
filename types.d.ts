

type CardCaseProps = {
    cardWrapperClass ?: string;
  };
  type LogoProps = {
    logoClass ?: string,
    height ?: number, 
    width ?: number
  };
  type SkipToMainProps = {
    sectionId : string,
    content : string
    customClass ?: string
  };
  
  export {CardCaseProps,LogoProps,SkipToMainProps};